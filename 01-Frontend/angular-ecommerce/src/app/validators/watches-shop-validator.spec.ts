import { WatchesShopValidator } from './watches-shop-validator';

describe('WatchesShopValidator', () => {
  it('should create an instance', () => {
    expect(new WatchesShopValidator()).toBeTruthy();
  });
});
